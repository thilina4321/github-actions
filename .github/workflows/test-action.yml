name: "Test project workflow"
on: push

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: "Get code"
        uses: actions/checkout@v3
      - name: "Install dependencies"
        run: npm ci
      - name: "Excute tests"
        run: npm test
